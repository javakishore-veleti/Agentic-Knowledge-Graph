[project]
name = "akg-service-core"
version = "0.1.0"
description = "Layering primitives shared by every AKG middleware service: object factories, request context, task pipeline, errors, paging"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/akg_service_core"]
