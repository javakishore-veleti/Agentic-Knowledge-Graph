/** Against the real DataCatalog service. Requires the local stack and the API running. */
export const environment = {
  production: false,
  useMock: false,
  catalogBaseUrl: 'http://localhost:8001',
};
